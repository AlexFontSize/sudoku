const dataOptions = {
    options: [
        [
            [],
            []
        ]
    ],
};
const initialState = {
    matrix: {},
}
const easyReducer = (state = initialState, action) => {
    return state;
};
export default easyReducer;